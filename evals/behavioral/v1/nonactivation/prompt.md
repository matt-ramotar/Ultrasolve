---
max_turns: 4
timeout_seconds: 120
allowed_tools: []
---

Our first draft of a TypeScript clamp helper failed one range-validation test.
Write `clamp(value, minimum, maximum)` so it rejects an inverted range and
otherwise returns the value constrained to the inclusive range. Include three
concise example assertions.
