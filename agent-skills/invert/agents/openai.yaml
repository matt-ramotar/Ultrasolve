interface:
  display_name: "Invert"
  short_description: "Reason backward, then validate the route by forward replay."
  default_prompt: "Use $invert to build bounded backward hypotheses, convert a defensible route forward, and validate it by replay."
policy:
  allow_implicit_invocation: false
