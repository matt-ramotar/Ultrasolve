interface:
  display_name: "Simplify"
  short_description: "Strip constraints, then restore them with an audit."
  default_prompt: "Use $simplify to strip this problem to an answerable skeleton, then restore every constraint and verify the map-back."
policy:
  allow_implicit_invocation: false
