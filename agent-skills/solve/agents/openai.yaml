interface:
  display_name: "Solve"
  short_description: "Route genuinely stuck problems through rigorous lenses."
  default_prompt: "Use $solve for this defined, stuck problem: [success, constraints, failed attempts, and evidence]. Preserve the existing contract, compare methods within the effort budget, and report the result with its verification limits."
policy:
  allow_implicit_invocation: true
