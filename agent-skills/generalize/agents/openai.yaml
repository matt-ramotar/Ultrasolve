interface:
  display_name: "Generalize"
  short_description: "Broaden or parameterize a result, then instantiate it."
  default_prompt: "Use $generalize to assess result-first broadening and parameterization for applicability, select a supported form, and instantiate it back to this problem."
policy:
  allow_implicit_invocation: false
