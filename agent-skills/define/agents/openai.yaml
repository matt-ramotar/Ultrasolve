interface:
  display_name: "Define"
  short_description: "State the unstated problem before any plan commits to it."
  default_prompt: "Use $define to sort the request, regress the goal to the mandate, and write a problem contract with observable success criteria before choosing a deliverable."
policy:
  allow_implicit_invocation: true
