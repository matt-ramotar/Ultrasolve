interface:
  display_name: "Define"
  short_description: "Define an unsettled problem proportionally."
  default_prompt: "Use $define to clarify this felt problem: [situation, desired outcome, constraints, authorized deliverable, and evidence]. Preserve binding decisions and prepare supported conditional work."
policy:
  allow_implicit_invocation: true
