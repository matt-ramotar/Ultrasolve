interface:
  display_name: "Restate"
  short_description: "Reframe a problem while preserving its invariants."
  default_prompt: "Use $restate to create materially different formulations, preserve the invariant ledger, and map the result back."
policy:
  allow_implicit_invocation: false
